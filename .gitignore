venv/
__pycache__/
*.pyc
config.json
# Log do agente — fica ao lado do config.json, inclusive rodando via
# `python main.py` na própria pasta do projeto.
*.log
.env
build/
dist/
.pytest_cache/

# Artefatos das verificações em ambiente gráfico. As capturas ficam FORA do
# versionamento porque este repositório é público e a janela mostra caminhos
# da máquina de quem rodou (pasta do usuário, diretório temporário). Rodar
# `scripts/verificacao/janela-agente.py` regenera todas.
scripts/verificacao/capturas/
